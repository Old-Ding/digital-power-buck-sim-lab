# 保护策略

## 保护项

```text
UVLO: 输入欠压
OVP: 输出过压
OCP: 输出过流
OTP: 过温
```

## 职责边界

保护检测只在 `protection_check()` 中完成。状态机不重复比较电压、电流、温度阈值，只消费保护层给出的故障码。

## 故障优先级

第一版优先级：

```text
OCP -> OVP -> UVLO -> OTP
```

优先级集中配置的原因是故障同时发生时必须有确定行为，避免不同模块给出冲突动作。
